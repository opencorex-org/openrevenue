import React from "react"; import { createRoot } from "react-dom/client"; import { BrowserRouter } from "react-router"; import { QueryClient, QueryClientProvider } from "@tanstack/react-query"; import { Button } from "@openrevenue/ui";
const areas=["Dashboard","Registration","Taxpayer profile","Tax registrations","Tax returns","Payments","Ledger","Documents","Notifications","Authorized representatives","Account settings"];
function App(){return <main><h1>OpenRevenue taxpayer portal</h1><p>Fictional demonstration environment</p><nav aria-label="Primary">{areas.map(x=><Button key={x}>{x}</Button>)}</nav></main>};
createRoot(document.getElementById("root")!).render(<React.StrictMode><QueryClientProvider client={new QueryClient()}><BrowserRouter><App/></BrowserRouter></QueryClientProvider></React.StrictMode>);
