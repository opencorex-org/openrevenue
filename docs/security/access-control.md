# Access control

OIDC establishes identity; the platform maps claims to local users. RBAC grants named permissions and ABAC narrows them using office, jurisdiction, assignment, ownership, tax type, approval limit, and data classification. Service accounts are workload-specific. Every sensitive allow/deny decision is auditable. Administrative roles require MFA and periodic access review.
