# Backup and recovery

Encrypt PostgreSQL base backups/WAL and versioned object-storage backups in a separate failure domain. Define retention by policy, monitor completion, and perform automated integrity checks plus scheduled isolated restores. Document recovery point/time objectives before production use.
