# Getting started

Install Go 1.24+, Node 22+, pnpm 10+, Docker, and golang-migrate. Run `make setup`, `make test`, `make docker-up`, then `make api`. Use fictional data only. PostgreSQL is on 5432, Mailpit UI on 8025, MinIO console on 9001, Prometheus on 9090, and Grafana on 3001.
