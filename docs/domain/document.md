# Document
Owns metadata, retention, classification, checksums, and object-storage keys. Upload adapters validate type/size, quarantine content, invoke malware scanning, and expose only authorized downloads.
